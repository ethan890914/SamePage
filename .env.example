# Public origin used by the browser to open the room WebSocket.
NEXT_PUBLIC_REALTIME_URL=ws://localhost:8787

# Server-only value. Never prefix secrets with NEXT_PUBLIC_.
ROOM_PASSWORD_PEPPER=replace-for-local-development
